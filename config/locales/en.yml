en:
  date:
    formats:
      default: '%m/%d/%Y'
      with_weekday: '%a %m/%d/%y'

  header:
    links:
      contact: "contact"
      projects: "projects"
      store: "store"
      stockists: "stockists"
      visuals: "visuals"

  time:
    formats:
      default: '%a, %b %-d, %Y at %r'
      date: '%b %-d, %Y'
      short: '%B %d'
      post: '%B %d, %Y'
